# Open Signing Key

This `.snk` file is for public strong name identity **only**.
Do not use for Authenticode signing or as a trust credential.

Consumers should verify origin via NuGet package signature, not the `.snk`.