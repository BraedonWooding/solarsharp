# Internal Source Generators

This holds a polyfill for RegexGeneratedAttribute (for now) but likely in future will hold more internal source generators.

This should not be exposed to the public API and is not intended for use outside of the library itself.
